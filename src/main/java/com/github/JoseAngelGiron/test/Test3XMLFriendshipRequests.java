package com.github.JoseAngelGiron.test;


import com.github.JoseAngelGiron.model.entity.User;
import com.github.JoseAngelGiron.model.session.UserSession;
import com.github.JoseAngelGiron.model.xmlDataHandler.ContactListHandler;

public class Test3XMLFriendshipRequests {
    public static void main(String[] args) {
        ContactListHandler contactListHandler = new ContactListHandler();

        //contactListHandler.create("lARG", user.getId()); //crearla al mismo tiempo que el usuario, refactorizar
        //contactListHandler.save(friendshipRequest.getSender());


        /**
         * FriendshipRequestHandler fr = new FriendshipRequestHandler();
         *         FriendshipRequest friendshipRequest = new FriendshipRequest("Pepe", "Jose");
         *         fr.create();
         *         fr.save(friendshipRequest);
         */

    }
}

